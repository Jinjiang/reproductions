{
  "name": "reproduce",
  "private": true,
  "scripts": {
    "dev": "node debug/init.mjs && node debug/run.mjs",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "vite": "^5.4.2"
  }
}
