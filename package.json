{
  "name": "reproduce",
  "private": true,
  "scripts": {
    "dev": "vite",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "vite": "8.0.0-beta.2",
    "vite-plugin-node-polyfills": "^0.24.0"
  }
}
