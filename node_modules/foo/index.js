import { jsx as _jsx } from "react/jsx-runtime";
import React from "react";

export function MyComponent() {
  const [count, setCount] = React.useState(0);
  return (_jsx("button", { onClick: () => setCount(c => c + 1), children: "hello world! " + count }));
}
