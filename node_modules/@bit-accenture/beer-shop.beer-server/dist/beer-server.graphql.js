import { gql } from 'graphql-tag';
export function beerServerSchema(beerServer) {
    const typeDefs = gql `#graphql
  
  type Beer {
    name: String
    type: String
    price: Float
  }

  type Query {
    beers: [Beer]
  }
`;
    const resolvers = {
        Query: {
            beers: async () => {
                return beerServer.listBeers();
            },
        },
    };
    return {
        typeDefs,
        resolvers
    };
}
//# sourceMappingURL=beer-server.graphql.js.map