export class Beer {
    constructor(
    /**
     * name of the instance
     */
    name, 
    /**
     * type of the beer
     */
    type, 
    /**
     * price for a single bottle.
     */
    price) {
        this.name = name;
        this.type = type;
        this.price = price;
    }
    /**
     * serialize a Beer into
     * a serializable object.
     */
    toObject() {
        return {
            name: this.name,
            type: this.type,
            price: this.price
        };
    }
    /**
     * create a Beer object from a
     * plain object.
     */
    static from(plainBeer) {
        return new Beer(plainBeer.name, plainBeer.type, plainBeer.price);
    }
}
//# sourceMappingURL=beer.js.map