import { jsx as _jsx } from "react/jsx-runtime";
// import type { ReactNode } from 'react';
import { useBeers } from '@bit-accenture/beer-shop.hooks.use-beers';
// export type TopBeersProps = {
//   children?: ReactNode;
// };
export function TopBeers() {
    const beers = useBeers();
    if (!beers)
        return _jsx("div", { children: "loading" });
    return (_jsx("div", { children: beers.map((beer) => {
            return _jsx("div", { children: beer.name }, beer.name);
        }) }));
}
//# sourceMappingURL=top-beers.js.map