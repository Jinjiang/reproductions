export function useConfig<T=object>(defaultConfig: T) {
  let config: T = defaultConfig;
  return {
    getConfig(): T {
      return config;
    },
    setConfig(newConfig: T) {
      config = newConfig;
    }
  }
}
