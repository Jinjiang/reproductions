import React from "react";
import { createRoot } from "react-dom/client";

export const render = (App: React.ComponentType) => {
  const rootElement = document.getElementById("app");
  if (rootElement) {
    createRoot(rootElement).render(<App />);
  }
}
