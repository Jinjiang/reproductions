import React from "react";
import { actions } from "header/header.browser";
import { routes } from "platform-a/platform-a.browser";
import { getConfig } from "./people";

actions.add(() => {
  console.log('Refreshed', getConfig());
})

routes.add({
  path: "/",
  element: (
    <div>
      <h1>Home</h1>
    </div>
  ),
});

routes.add({
  path: "/people",
  element: (
    <div>
      <h1>People</h1>
    </div>
  ),
});
