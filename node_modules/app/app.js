import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import React from "react";
import { createBrowserRouter, RouterProvider, } from "react-router-dom";
export const layout = new Map();
export const routes = new Set();
export const App = () => {
    const router = createBrowserRouter(Array.from(routes));
    return (_jsxs("div", { children: [_jsx("div", { children: Array.from(layout, ([key, value]) => (_jsx("div", { id: key, children: value }, key))) }), _jsx(RouterProvider, { router: router })] }));
};